#include <stdio.h>

int main(void) {
    int x = 10;
    int *p = &x;
    int **pp = &p;

    // TODO: complete the output statements
    printf("x = %d\n", ???);
    printf("x via p = %d\n", ???);
    printf("x via pp = %d\n", ???);

    return 0;
}
